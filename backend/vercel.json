{
  "version": 2,
  "env": {
    "BOLETO_GATEWAY_TIMEOUT_MS": "60000"
  },
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.ts"
    }
  ]
}
